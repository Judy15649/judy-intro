services:
  - type: web
    name: judy-intro
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: CLAUDE_API_KEY
        sync: false
